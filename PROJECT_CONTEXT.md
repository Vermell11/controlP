# ControlP Context

ControlP is a local-first visual Agentic Project OS for Andres Ortega. It reads the Cerebro Obsidian vault as the project index, resolves local project folders under `/code`, and visualizes Git, Graphify, sessions, health, and next steps in a V.A.U.L.T.-style dashboard.

Current state: V1.5.2.1 delivered; V1.5.3 in progress. The semantic/RAG boundary remains read-only and deterministic-first. The first authorized-action boundary now records catalogued proposals as append-only snapshots with local identity, exact SHA-256 preview binding, expiry, idempotency and a filesystem lock; only a separate confirmation can move them to `queued`. No mutation runner is enabled yet. Voice lifecycle recovers wake/PTT after TTS/errors and Command Deck has deterministic views. Whisper exposes a localhost-only control for `stt/run.sh`. The model has no filesystem, write or credential access. Data contract `SCHEMA_VERSION 2`.

Navigation for new sessions: `CONTEXT_MAP.md` (repo root) maps every module — purpose, files, extension points, invariants, and a "to change X touch only Y" table; it is kept current at every session close. Architecture recipes live in Obsidian (`Proyectos/ControlP/Arquitectura/Routers y extensión`); a brief schematic manual with the Obsidian/Notion migration plan is in `docs/manual.md` (deliberately not in `public/`).

Quality gates: `npm run check`, `npm test`, Gitleaks, online `npm audit` and a production Webpack build; registry enforces id/slug uniqueness. A root PostCSS override keeps Next on the audited PostCSS 8.5.17 release until its nested pin is updated upstream. Turbopack hangs in the iCloud folder, while Webpack completes — known environmental debt; move the repo out of iCloud. Mic requires localhost/https.
Reto actual: V1.5.3 — add authorized actions behind the mandatory `propuesta → vista previa → confirmación → ejecución → traza` pipeline, enforced by runners/adapters rather than prompts. Harden `/api/intents` before real execution. Roadmap in `Proyectos/ControlP/Roadmap`; data architecture in `Proyectos/ControlP/Arquitectura/Datos`.

Pointers: `CONTEXT_MAP.md`, `app/components/voice/commands.ts`, `app/components/voice/useSpeech.ts`, `app/api/stt/service/route.ts`, `lib/assistant.ts`, `lib/assistant-semantic.ts`, `lib/assistant-rag.ts`, `lib/assistant-knowledge.ts`, `app/api/assistant/`, `lib/schema.ts`, `lib/adapters/`, `docs/manual.md`, `graphify-out/GRAPH_REPORT.md`.
